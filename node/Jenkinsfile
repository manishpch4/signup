pipeline {
    agent any

    stages {
        stage('Listing the files') {
            steps {
                  sh 'ls'

            }
          
        }

        stage('View the content') {
            steps {
                sh 'cat main.tf'

            }
            
        }    
    }
}